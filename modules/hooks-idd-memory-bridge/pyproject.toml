[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "amplifier-module-hooks-idd-memory-bridge"
version = "0.1.0"
requires-python = ">=3.11"

[project.entry-points."amplifier.modules"]
hooks-idd-memory-bridge = "amplifier_module_hooks_idd_memory_bridge:mount"
